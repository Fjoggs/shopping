dependencies:
  cache_directories:
    - ~/.meteor
  override:
    - curl https://install.meteor.com | /bin/sh
    - npm install

test:
  override:
    - meteor npm test

checkout:
  post:
    - git submodule update --init
